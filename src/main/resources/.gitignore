
/.project
/.settings
/pom.xml.releaseBackup
/release.properties
/target
/pom.xml.next
/pom.xml.tag
d3demo/

# this is some sort of a "hack" because git saves only files, not folders, so if 
# we wanted this folder to be present, git must see a file in it
